const resourceForm=document.querySelector('#resource-form');
if(resourceForm){resourceForm.addEventListener('submit',event=>{event.preventDefault();if(!resourceForm.reportValidity())return;const isHomepage=resourceForm.dataset.resourceContext==='home';resourceForm.reset();const status=document.querySelector('#resource-status');status.hidden=false;status.textContent=isHomepage?'Preview complete. Nothing was sent or saved. Working-kit delivery will connect at launch.':'Preview complete. Nothing was sent or saved. The PDF link on this page opens the working kit.';status.tabIndex=-1;status.focus()})}
